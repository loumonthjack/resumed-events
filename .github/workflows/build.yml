name: Deploy API

on:
  workflow_dispatch:
  push:
    branches: [ main ]
    tags:
      - 'v*.*.*'
env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ghcr.io/loumonthjack/resumed-events

jobs:
  deploy:
    runs-on: self-hosted
    permissions:
      contents: read
      packages: write
    environment: production
    steps:
    - name: Stall  20 seconds
      run: sudo -i 
    - name: Check Container Status
      run: cd /home/app/resumed-events
    - name: Delete PM2 Process
      run: sudo pm2 delete resumed-events
    - name: Pull Latest Code
      run: sudo git pull origin main
    - name: Start PM2 Process
      run: sudo pm2 start npm --name resumed-events -- run start:dev
